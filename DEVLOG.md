# mmr-cards 개발 일지

한국어 ⇄ 일본어 플래시카드 단어장 앱. 하루 만에 0에서 배포까지 만들고 이후 다듬어 온 기록.

- **라이브**: https://mmr-cards-jp.vercel.app
- **저장소**: https://github.com/stardust88917/mmr-cards
- **폴더**: `PROJECTS/mmr-cards`

### 날짜별 기록
| 날짜 | 내용 |
|---|---|
| [2026-08-19](#2026-08-19--초기-구축) | 초기 구축 — 사전 연동·배포·발음·가나 탭·튜토리얼·디자인·다국어·PWA |
| [2026-08-20](#2026-08-20--ui-개선--버그-수정) | 배포 후 피드백 반영 — 학습 화면 UI, 후리가나, 버그 3건 |
| [2026-08-21](#2026-08-21--단어-목록-2단-구조--play-스토어-준비-시작) | 단어 목록 2단 구조 · 접근성 WCAG AA · Play 스토어 준비 |

---

# 2026-08-19 — 초기 구축

## 1. 개요

국립국어원 **한국어기초사전 Open API**로 한국어 단어를 검색하면 일본어 뜻·발음·예문을 자동으로 채워 주는 플래시카드 앱.

- **한국어 사용자** → 일본어 단어를 외우는 앱
- **일본어 사용자** → 한국어 단어를 외우는 앱 (UI·튜토리얼 전부 일본어)
- 디자인은 [gen-sports26](https://github.com/stardust88917/gen-sports26)의 두 테마를 참고 → 최종적으로 **朱墨(미니멀)** 방향으로 확정

---

## 2. 아키텍처

```
mmr-cards/
├─ index.html            프론트엔드 (단일 파일: HTML+CSS+JS, 무프레임워크)
├─ api/
│  ├─ search.js          krdict 검색 프록시 (키 숨김 + CORS 우회)
│  ├─ view.js            krdict 상세 프록시 (예문 + 발음 file_no)
│  └─ audio.js           krdict 발음 음성 프록시
├─ dev.mjs               로컬 개발 서버 (Vercel 없이 실행)
├─ manifest.webmanifest  PWA 매니페스트
├─ sw.js                 서비스 워커 (오프라인 캐시)
├─ icon.svg              앱 아이콘 (주색 単)
├─ vercel.json · package.json · .env.example · .gitignore
└─ .env.local            KRDICT_KEY (gitignore)
```

- **왜 프록시?** 브라우저에서 사전 API를 직접 부르면 ① CORS로 막히고 ② 인증키가 노출됨. Vercel 서버리스 함수가 서버에서 대신 호출 → XML 그대로 전달 → 브라우저가 `DOMParser`로 파싱. 키는 서버 환경변수(`KRDICT_KEY`)로만 주입.
- **비자명 함정**: krdict는 비브라우저 요청을 `400 Request Blocked`로 차단 → 프록시 `fetch`에 브라우저 User-Agent 필수.

---

## 3. 오늘의 마일스톤 (시간순)

### ① 앱 뼈대 + 사전 연동
- 한국어기초사전 API 스펙 확인: `search`(발음 O, 예문 X), `view`(예문 O, `pronunciation_info`에 발음 음성), `trans_lang=2`=일본어, 응답 XML
- 프론트(카드 플립·덱·localStorage) + 프록시(search/view) 구현
- 로컬 브라우저에서 렌더·검색·추가 전 과정 검증

### ② 프로젝트 정리 & 배포
- 폴더 `jp-ko-vocab` → `mmr-cards`로 이동, GitHub 저장소 연결·push
- Vercel 배포 → **검색이 500**: 환경변수 `KRDICT_KEY` 미설정이 원인 → Production에 등록 + 재배포로 해결
- **로컬 실행 문제**: Vercel CLI 미설치라 `npm run local`(=`dev.mjs`)로 로그인 없이 실행하도록 개선. (엔터프라이즈 보안이 `.bat`를 막아 `.bat` 런처는 폐기)

### ③ 발음 듣기 🔊
- krdict가 **실제 사람 녹음**을 제공(`oraginalDownload.do?file_no=`)하는 걸 발견 → `api/audio.js` 프록시 추가
- 한국어=krdict 녹음(없으면 브라우저 TTS 대체), 일본어 뜻=브라우저 TTS(ja-JP)
- 카드 앞/뒤에 스피커 버튼

### ④ 가나(かな) 학습 탭
- 오십음도 표(청음·탁음·요음) + 히라가나⇄가타카나 전환(유니코드 오프셋) + 발음 재생
- **퀴즈**: 글자↔읽기 4지선다, 실시간 채점

### ⑤ 첫 접속 튜토리얼
- 4단계 온보딩 모달(환영→단어추가→학습→가나), 첫 방문에만 표시, 도움말(?) 재열람
- '단어 추가' 단계에 **미니 시각 데모**(검색창→결과카드→＋추가)

### ⑥ 디자인 패스 — 朱墨(미니멀)
- 레퍼런스에서 3가지 팔레트 방향 뽑아 비교([팔레트 보드](https://claude.ai/code/artifact/aadf81db-3e8f-4c18-bf72-01cfe80166ee)) → **朱墨** 선택
  - 크림 종이 `#F1ECE3` + 먹 `#1A1815` + 주색 `#C8503C`
- **明朝체**(Zen Old Mincho / Noto Serif KR)로 단어·로고
- 모든 이모지 → **인라인 SVG 라인 아이콘**
- **인장(印) 도장 효과**: '외웠어요' 누르면 주색 「済」 도장이 찍히는 애니메이션 + 외운 카드 상시 인장

### ⑦ 버그 수정 — 빈 카드
- 일본어 직역어(`trans_word`)가 없는 단어(예: 밥상머리)는 `일→한`에서 빈 카드
- **수정**: 직역어 없으면 일본어 뜻풀이(`trans_dfn`)로 대체(`jaMain` 헬퍼). 이미 추가된 카드도 자동 정상화

### ⑧ 다국어 (한/일)
- 첫 실행 시 **언어 선택 화면**(한국어/日本語)
- 일본어 선택 → UI·튜토리얼(3단계) 전부 일본어, **가나 탭 숨김**(일본인은 가나를 알므로), 품사/등급도 번역
- 언어 토글 버튼(`日`/`한`), `notranslate`로 Chrome 자동번역 방지

### ⑨ 모바일 최적화 + PWA
- 상단 탭 유지 + 터치 타깃 확대·16px 입력·세이프에어리어·눌림 피드백
- **카드 스와이프**: 오른쪽=외움 / 왼쪽=다시 / 탭=뒤집기 (드래그 중 초록·빨강 테두리)
- **PWA**: `standalone` 매니페스트 + 서비스 워커(오프라인) + SVG 아이콘 + 상태바 색 → 안드로이드에서 "홈 화면에 추가"로 앱처럼 실행

---

## 4. 실행 방법

```bash
cd PROJECTS/mmr-cards
npm run local      # http://localhost:3000 (Vercel 불필요)
```
> `.env.local`에 `KRDICT_KEY=발급받은_인증키` 필요. 배포는 Vercel 환경변수에 `KRDICT_KEY` 등록 후 자동.

### 다른 PC 에서 이어받을 때
`.env.local` 은 gitignore 대상이라 리포에 없다. 클론 후 직접 만들어야 사전 검색이 동작한다.

```bash
git clone https://github.com/stardust88917/mmr-cards.git
cd mmr-cards
echo "KRDICT_KEY=발급받은_인증키" > .env.local
npm run local
```

| 경로 | 용도 |
|---|---|
| `index.html` | 앱 전체 (HTML+CSS+JS 단일 파일, 무프레임워크) |
| `api/` | krdict 프록시 — search / view / audio |
| `tools/icon-gen.html` | 앱 아이콘 생성기. 브라우저로 열면 됨(서버 불필요) |
| `privacy.html` | 개인정보처리방침 → 라이브에서는 **`/privacy`** (`cleanUrls`) |
| `.well-known/assetlinks.json` | Digital Asset Links. SHA-256 지문 자리표시자 상태 |

## 5. 안드로이드 설치
크롬으로 라이브 접속 → 메뉴(⋮) → **앱 설치 / 홈 화면에 추가** → 전체화면 앱 + 오프라인 동작

---

## 6. 남은 것 / 다음 후보
- 일본어 사용자용 **한글(자모) 학습 탭** (가나 탭의 한글 버전)
- 먹빛 라인 일러스트(스미 모티프)로 빈 화면·여백 마감
- **Play 스토어** 등록 (TWA — Bubblewrap/PWABuilder로 APK/AAB 래핑)
- 아이콘 PNG 버전(현재 SVG — 구형 안드로이드 대비)
- 간격 반복(SRS) 복습 스케줄

---

## 7. 주요 커밋 (2026-08-19 초기 구축)
- 일본어-한국어 단어장 초기 버전 / 프로젝트 mmr-cards 정리
- 발음 듣기(스피커) · 가나 학습 탭 · 첫 접속 튜토리얼
- 컬러 팔레트 朱墨 · 明朝체+라인 아이콘 · 인장 도장 효과
- 빈 카드 버그 수정 · 다국어(한/일) · 모바일+PWA

---

# 2026-08-20 — UI 개선 & 버그 수정

배포 후 실기기 피드백 반영.

### 버그 수정
- **스피커 탭 시 카드 뒤집힘** — 뒤집기를 포인터 탭으로 바꾼 뒤 스피커의 click 차단만으론 부족 → 스피커에서 pointer 이벤트 전파 차단(소리만 재생). `e937c06`
- **nav 높이 탭별 불일치** — `body`를 flex 컬럼으로 바꾼 뒤 콘텐츠 긴 페이지(단어관리·가나)에서 nav가 flex-shrink로 39px로 찌그러짐 → `nav { flex: none }` 고정(모든 탭 64px). `74a3019`
- **'공부하다' 후리가나 미표시** — 옛 더미 시드가 독음 없는 `勉強する`로 저장돼 있던 것 → 로드 시 `べんきょうする【勉強する】`로 자동 보정(마이그레이션). `633eb84`

### 학습 화면 UI
- **덱 칩 → 드롭다운** + 우측 작은 셔플 아이콘, 긴 카테고리명 14자+`…`로 잘라 표시(필터 값은 원본). `2080a0a`
- **상단 単語帳 로고 제거 → 하단 중앙 footer**(明朝 주색) + API 상태등 하단 이동 + `© stardust.lab · All rights reserved.` `b288eea` `6166bd5`
- **이전/다음을 카드 안 오버레이(‹ ›)로**, 좌우 스와이프 = 이전/다음, **뒤집기 버튼 제거**(탭=뒤집기). `b288eea` `6166bd5`
- **트랙패드 가로 플리킹(wheel) 지원** — 브라우저에서 좌우 플리킹이 pointer가 아닌 wheel로 잡히던 문제 보완. `6166bd5`
- **프로그레스 바 제거 → 카드 좌측 하단 `1 / 8` 카운트**, 상단 여백 정리로 카드 위로. `7c153ad`
- **상단 탭 선택 디자인**을 세그먼트(전체·모름·외움)처럼 각지게(radius 8). `6166bd5`
- **내보내기/가져오기(JSON) 삭제** — 일반 사용자에 불필요. `74a3019`

### 학습 편의
- **한자 후리가나(독음)** — 일본어 번역어 `히라가나【漢字】`를 한자 위 후리가나(ruby)로 표시. 예: `勉強する` 위 `べんきょうする`. `74a3019`

### 확인된 사항 (API)
- krdict `pronunciation`은 **한국어 발음만** 제공, 일본어용 로마자/IPA 발음기호 필드는 **없음**. 일본어 히라가나 읽기는 번역어(`かな【漢字】`)에 포함되어 후리가나로 활용. 로마자 표기가 필요하면 히라가나→로마자 클라이언트 변환으로 가능.

*Generated with Claude Code · updated 2026-08-20*

---

# 2026-08-21 — 단어 목록 2단 구조 · Play 스토어 준비 시작

## 단어 목록 2단 구조 (기본 / 전체 보기)

### 문제
관리 탭 '내 단어' 목록이 **무한 스크롤이 아니라 전체 렌더**였다. `db.words`를 통째로 `innerHTML`에 넣고 항목마다 수정·삭제 리스너를 개별 바인딩 → 단어 N개면 DOM ~10N, 리스너 2N. 상한이 없어 500개부터 체감 렉, 1,000개면 스크롤 10만 px(≈110화면).

진짜 병목은 스크롤 길이가 아니라 **검색이었다.** `listSearch`에 디바운스가 없어 키 한 번마다 전체 목록을 파괴·재생성 + 리스너 전부 재바인딩.

> 저장소는 병목이 아님 — localStorage 5MB / 단어당 JSON ~200B → 2만 개까지 여유. 순전히 렌더링 문제.

### 설계 판단
- 훑어보기는 **학습 탭이 이미 하고 있다**(덱 드롭다운 + 상태 세그먼트 + 스와이프). 관리 탭 목록이 그걸 리스트로 복제하던 셈 → 상시 노출이 중복.
- 관리 탭에서 목록이 실제로 하는 일은 ① 방금 추가한 게 들어갔나 확인 ② 특정 단어 찾아 수정/삭제. ①은 짧은 목록, ②는 검색이 더 잘한다.
- 그래서 **페이지네이션을 버리는 게 아니라 '전체 보기' 모드 안으로 격리**했다. 평소엔 안 걸리고, 사용자가 명시적으로 들어갔을 때만 작동.

### 결과 — 두 모드
| | 기본(recent) | 전체 보기(all) |
|---|---|---|
| 목적 | 추가 확인 | 찾아서 손보기 |
| 노출 | 최근 10개 (총 30개 이하면 전부) | 20개 × 페이지 |
| 검색·덱·정렬 | 없음 | 있음 |

- 어느 모드든 **한 번에 그리는 항목 수에 상한**이 있어 단어가 수천 개여도 렌더 비용이 일정.
- 전체 보기 안에서는 단일 파이프라인 — `덱 필터 → 검색 → 정렬 → 페이지 슬라이스`. 검색 결과 200건도 그냥 10페이지로 나뉘어 특수 케이스가 없다.
- 단어가 10개 이하면 전체 보기 버튼 자체를 숨김 → 신규 사용자에겐 예전과 동일하게 보인다.

### 진입 경로
- 목록 하단 `단어장 전체 보기 (N개)` 버튼
- **단어장 칩 클릭** — 기존엔 `cursor:default`로 눌러도 아무 일이 없던 걸 그 덱의 전체 보기 진입점으로. `전체` 칩 추가, 현재 보는 덱은 active 표시. ✕(삭제)는 `stopPropagation`으로 분리

### 새 단어 강조
- 이번 세션에 추가한 단어는 목록에서 주색 왼쪽 보더 + tint 배경 + `NEW` 배지(`.item.is-new`), 처음 그려질 때 fade-in
- 추가 시 **기본 모드로 되돌림** — 전체 보기 중에 담아도 새 단어가 맨 위에 보이게
- 사전 결과 카드의 `✓ 추가됨`은 즉각 피드백, 목록 강조는 나중에 내려봐도 남는 흔적 — 역할 분리

### 성능
- **이벤트 위임** — `#wordList`에 리스너 1개(`closest`)로 수정·삭제 처리. 리스너 2N → 1
- **검색 디바운스 180ms**
- 정렬은 `createdAt` 내림차순, 동률이면 저장 순서로 보조 (옛 시드 대비)

### 자잘한 것
- 목록 헤더 이동 시 **sticky nav(64px) 오프셋 보정** — `scrollIntoView`는 nav 밑으로 파고들어감. `scrollListTop()`으로 통일
- 모드/페이지 전환은 내용이 통째로 바뀌므로 smooth 대신 **즉시 스크롤**
- 정렬 `가나다순`은 `localeCompare(_, "ko")`, 일본어 UI에선 `ハングル順`
- i18n 키 추가(ko/ja): `mngAllWords` `listRecent` `listBack` `btnViewAll` `sortNew` `sortAz` `tagNew` `listAllEmpty` `pageInfo`

### 검증
137개 시드로 브라우저 확인 — 기본 10개/부제/전체 보기 버튼, 페이저(`‹ 1 … 3 [4] 5 … 7 ›`), 덱·정렬·검색(단어·발음·일본어 뜻·예문) 조합, 뒤로가기 시 검색어 초기화, 덱 칩 진입, 새 단어 강조 + 기본 모드 복귀, 수정/삭제 위임, 다크모드, 일본어 UI 전체 번역, 360px 폭 오버플로 없음, 단어 8개일 때 버튼 숨김. 콘솔 에러 없음.

## Play 스토어 출시 준비 ① 접근성 · PWA 자산

### 라이선스 확인 (선결 과제였음)
국립국어원 한국어기초사전 오픈 API는 **공공누리 제1유형** — 출처만 표시하면 상업적 이용·변형까지 자유. 스토어 출시에 법적 걸림돌 없음. 대신 **출처 표시가 의무**라 푸터에 고지 추가:

```
Copyright © stardust.lab. All rights reserved.
본 서비스는 국립국어원 한국어기초사전 오픈 API를 활용하였습니다. (공공누리 제1유형)
```
일본어 UI에도 대응(`footAttr`). 푸터를 가로 배치에서 세로 2줄로 변경.

### PWA 자산 (TWA 빌드에 필수)
manifest에 SVG 아이콘 하나만 있었고 그걸 `any`와 `maskable`에 동시 지정 → 원형 크롭에서 `単` 끝이 잘림. Chrome 캔버스로 明朝 폰트를 실제 렌더해 PNG 생성:

| 파일 | 용도 |
|---|---|
| `icon-192.png` `icon-512.png` | manifest `any` |
| `icon-maskable-512.png` | safe zone 안으로 축소(40%)한 별도 버전 |
| `playstore-512.png` | 스토어 등록정보 |

`apple-touch-icon`도 PNG로(iOS는 SVG 미지원), SW 캐시 `v1`→`v2`로 올리며 PNG 포함.

### 접근성 감사 (axe-core 4.10)
라이트/다크 × 한/일 × 3탭 전수 검사. **최종: 전 조합 violation 0.**

**critical — 폼 라벨이 전혀 연결돼 있지 않았음**
`<label>`에 `for`가 하나도 없어 `#fWord`는 접근 가능한 이름이 아예 없었고, `#fDeck`·`#targetDeck` select도 무명. 나머지 필드는 placeholder 덕에 가려져 있었을 뿐 같은 문제. → 전 필드 `for`/`id` 연결 + `data-i18n-al`(aria-label) 처리기 추가.

**serious — 색 대비**
`--text-light: #8C8578`가 `.desc .count .tab .kr label .item .ie` 등 앱 전반에 쓰이는데 surface-2 위에서 **2.92:1**. 강조색 위 흰 글자도 전부 미달.

| 토큰 | 변경 | 결과 |
|---|---|---|
| `--text-light` (light) | `#8C8578` → `#6C665C` | 2.92 → **4.54** |
| `--primary` (light) | `#C8503C` → `#C84F3B` | 4.49 → **4.53** |
| `--success` (light) | `#6B8659` → `#657E54` | 4.05 → **4.50** |
| `--on-accent` (신규) | light `#FFFFFF` / dark `#1A1815` | dark 3.41 → **5.20** |
| `--logo` (신규) | light `#AE4230` / dark `#DA6A50` | 3.85 → **4.93 / 5.40** |

다크는 강조색을 어둡게 죽이는 대신 **밝은 주색 위에 먹색 글자**를 얹는 쪽을 택했다(朱墨 성격 유지 + 5.2:1). 하드코딩된 `color:#fff` 13곳을 `--on-accent`로 교체.

**함정 둘**
- `opacity`로 흐리게 한 것들이 전부 대비를 깎고 있었음. 덱 칩 개수(`opacity:.6`)는 0.9로 올려도 라이트에서 4.30이라 통과 불가 → 불투명도를 버리고 **굵기(400)로 구분**. `.foot-attr`의 `.92`도 제거.
- `.card-count`는 카드 **앞뒤 양면에 걸쳐** 있어 한 색으로는 양쪽을 못 맞춘다. `--card-back-text`로 바꿨더니 밝은 앞면에서 안 보이는 역효과 → `.card-inner.flipped ~ .card-count`로 **뒤집힘 상태에 따라 전환**(앞 5.32 / 뒤 6.11).

**moderate**
- `<main>` 랜드마크·`<h1>` 부재 → 세 페이지를 `<main>`으로 감싸고 `.sr-only` h1 추가. `region` 위반 39건이 함께 해소
- 가나 탭 `h1 → h3` 단계 건너뜀 → `h2`로 (CSS 선택자도 함께)

**키보드 · 터치**
- 버튼에 `:focus-visible` 스타일이 없었음 → 주색 3px 링 + offset. 강조색 배경 위에서는 묻히므로 `--text`로 반전
- 수정/삭제 `.sq` 32×32 → **40×40** (Android 권장 48dp에 근접)
- `maximum-scale=1` 제거 — 확대 차단은 접근성 위반

### 그 외
- `privacy.html` 신규 — Play Console 필수 항목. 한/일 병기, 수집 없음·기기 내 저장·외부 전송(검색어) 명시

## Play 스토어 출시 준비 ② 확정 사항

### 패키지명 (확정 — 변경 불가)
```
app.stardust.mmrcards
```
한번 스토어에 올리면 **영구히 변경·재사용 불가**. 바꾸려면 새 앱으로 등록해야 하고 기존 사용자는 자동 업데이트를 못 받는다(설치 수·리뷰도 리셋).

쓰이는 곳: 스토어 URL(`?id=app.stardust.mmrcards`) · 기기 내 앱 식별 · 데이터 경로 · **assetlinks.json**(패키지명 + 서명 SHA-256 을 도메인에 등록해야 TWA 주소창이 사라짐) · 업데이트 검증(패키지명 + 서명키가 둘 다 같아야 함).

> 하이픈은 못 쓰므로 `mmr-cards` → `mmrcards`. 관례는 통제하는 도메인의 역순이며 `io.github.stardust88917.mmrcards` 도 후보였으나 짧은 쪽으로 결정.

### 빌드 방식 (확정)
**PWABuilder** — 웹에서 URL 입력만으로 서명된 AAB 생성. 이 PC에 JDK/Android SDK 가 없어 Bubblewrap 은 환경 구축부터 필요.

### 계정 (확정)
**개인 개발자 계정 신규 개설** → 프로덕션 출시 전 **테스터 20명 × 14일 연속 비공개 테스트** 필수. 일정에서 가장 큰 변수이므로 테스터 모집을 먼저 시작.

---

## Play 스토어 출시 준비 ③ 라이브 검증

라이브 배포본(https://mmr-cards-jp.vercel.app)에서 TWA 전제 조건을 실측.

| 항목 | 결과 |
|---|---|
| HTTPS | 200, HTTP → HTTPS **308 리다이렉트** |
| manifest | `application/manifest+json` 로 서빙, 필수 필드 누락 0, display=standalone |
| 아이콘 | 192 / 512 / maskable / 스토어용 전부 200 |
| Service Worker | 등록·활성(scope `/`), 캐시 `mmr-cards-v2`, 셸 6개 적재 |
| SW 예외 처리 | `/api/` 는 캐시하지 않음(확인) |
| 개인정보처리방침 | **`/privacy`** (`cleanUrls: true` 라 `/privacy.html` 은 308) |
| `.well-known/assetlinks.json` | 200, `application/json` |

### 반응형 전수 점검
320 / 360 / 390 / 412px × 3탭 가로 오버플로 검사 → **360px 관리탭에서 `.im` 6px 초과** 발견. 공백 없이 이어지는 일본어 뜻(`うみ【海】。かいよう【海洋】`)이 안 끊긴 것 → `.item .info { overflow-wrap: anywhere }`. 나머지 폭은 정상.

### assetlinks.json — 지문만 미완
서명 SHA-256 은 Play App Signing 등록 후에야 발급되므로 자리표시자로 배치했다. **이번 배포의 목적은 Vercel 이 점(.)으로 시작하는 디렉터리를 서빙하는지 확인**하는 것이었고(정적 호스트가 흔히 제외한다), 정상 서빙됨을 확인했다. TWA 빌드 단계에서 발견하면 늦다.

지문을 받은 뒤 `REPLACE_WITH_PLAY_APP_SIGNING_SHA256` 한 줄만 교체하면 된다.

---

## Play 스토어 출시 준비 ④ 앱 아이콘 3안 (결정 대기)

초기 `単` 아이콘은 확인 없이 만든 것이었고, **일본어 사용자가 한국어를 배우는 앱**이라는 성격이 드러나지 않았다(単은 단어장의 単일 뿐 언어 방향성이 없음). 키컬러만으로 3안 제작.

| 안 | 구성 | 성격 |
|---|---|---|
| **A** | 주색 바탕 + 크림 한글 「가」 | 「가」는 가나다의 첫 글자라 한글을 몰라도 한국어로 읽힌다. 한 글자뿐이라 **48dp에서 가장 또렷** |
| **B** | 먹/주색 대각 분할 + 「あ」 「가」 | 일본어↔한국어를 오가는 앱임이 바로 보인다. 다만 48dp에선 두 글자가 작아짐 |
| **C** | 크림 바탕 + 주색 明朝 「韓」 | 일본인은 韓国語의 韓을 즉시 한국어로 읽는다. 초기 `単`의 결을 잇는 안 |

**추천 A** — ① 아이콘이 실제 소비되는 48dp에서 가장 확실히 읽히고 ② 한자 韓은 일본인에게만 통하지만 한글 「가」는 양쪽에 통하므로 앱이 양방향인 것과 맞고 ③ 주색 바탕이라 스토어 목록에서 채도로 튄다.

### 아이콘 생성기 — `tools/icon-gen.html`
브라우저로 파일을 그냥 열면 되고(서버 불필요) 안별로 **내려받기** 버튼이 4종 세트(192 / 512 / maskable-512 / 스토어용)를 저장한다. 받은 파일을 프로젝트 루트에 덮어쓰면 교체 완료.

캔버스는 폴백 폰트로 한번 그리면 되돌릴 수 없으므로 `document.fonts.ready` 이후에 렌더한다. 초기 `単` 안도 참고용으로 남겨 뒀다.

> **미결** — 어느 안으로 갈지 정해지지 않았다. 현재 루트의 PNG 4개는 여전히 초기 `単` 버전.

### 서명키 보호
`.gitignore` 에 `*.keystore *.jks *.p12 *.pepk keystore* signing-key* *.aab *.apk` 추가. 서명키가 유출되면 **앱 업데이트 권한을 영구히 잃는다**(같은 키로만 업데이트가 인정됨).

### 브랜치 전략 — main 단일 유지
TWA 는 **배포된 웹사이트를 감싸는 것**이라 이 리포에 안드로이드 소스가 없다(AAB 는 PWABuilder 가 라이브 URL 에서 생성). 여기서 한 작업(PNG 아이콘·접근성·출처 고지·개인정보처리방침)은 전부 **웹 앱 자체의 개선**이라 격리할 이유가 없고, 무엇보다 `assetlinks.json` 은 **프로덕션 도메인에 실제로 떠 있어야** 검증되므로 main 에 있어야 한다. 릴리스 시점은 브랜치 대신 **태그**로 표시한다.

---

## Play 스토어 출시 준비 ⑤ 아이콘 확정 — B6

**B안(あ + 가 대각 분할)** 채택 후 색 조합 6가지를 비교해 **B6(주색 / 크림)** 으로 확정.

### 색을 가른 기준 — 대비
크림 글자를 주색 위에 올리면 **3.8:1**, 먹 위에 올리면 **15.1:1**. 4배 차이라 48dp 에서 한 톤 물러나 보인다. 즉 **어느 글자를 주인공으로 둘지**의 문제였고, 앱이 "일본인이 **한국어**를 배우는" 것이므로 「가」가 주인공이어야 한다.

| | 상단좌 / 하단우 | あ | 가 | 판단 |
|---|---|---|---|---|
| B1 | 먹 / 주색 | 15.1 | 3.8 | 「가」가 물러남 |
| B2 | 주색 / 먹 | 3.8 | 15.1 | 먹·주색이 둘 다 어두워 48dp 원형에서 경계가 덜 읽힘 |
| B3 | 크림 / 주색 | 15.1 | 3.8 | 「가」가 물러남 |
| B4 | 크림 / 먹 | 3.8 | 15.1 | 또렷하나 주색이 「あ」 한 글자에만 남아 색감이 죽음 |
| B5 | 먹 / 크림 | 15.1 | 3.8 | 「가」가 물러남 |
| **B6** | **주색 / 크림** | 3.8 | **15.1** | **채택** — 주색 면적이 넓어 채도로 튀고 「가」가 가장 선명. 세 키컬러가 모두 살아있는 유일한 조합 |

### Play 스토어 경쟁 아이콘 조사
일본 스토어 「韓国語 単語」 검색 결과 상위 11개를 확인.

- **대각 분할로 두 문자 체계를 병치한 아이콘은 없다.** B6 의 구성은 이 목록에서 유일
- 캐릭터·일러스트가 다수(11개 중 7개). 타이포그래피 기반은 3개뿐이라 오히려 차별화된다
- 팔레트가 겹치는 건 둘 — 「韓国語単語トレーニング」(빨강+검정+흰색, 4글자 세로 배치)와 Mondly(주황 바탕 지구본). **둘 다 구성이 완전히 달라 혼동 위험은 낮다**
- **단일 한글 「한」을 쓰는 앱이 이미 있다**(毎日3分で韓国語, 파란 바탕). 초기 후보였던 **A안(주색 바탕 「가」)과 개념이 겹쳤을 것** — B 를 고른 것이 결과적으로 맞았다
- 태극기 문양을 쓰는 앱이 여럿(Drops·Eggbun·TOPIK). 우리는 사용하지 않음

### 생성기 갱신 — `tools/icon-gen.html`
설정 기반으로 재작성. B1~B6 색 조합 + 검토했던 A/C/초기 単 안을 모두 담고, **채택안(B6)을 표시**하며 각 안의 글자/바탕 대비를 함께 보여준다(3:1 미만이면 경고색). 안별 내려받기 버튼이 4종 세트를 저장.

도구가 생성한 B6 512px 를 설치된 `icon-512.png` 와 픽셀 대조 → **26만 픽셀 중 6개만 최대 3/255 차이**(안티앨리어싱 비결정성). 즉 도구로 언제든 동일하게 재현 가능.

---

## 남은 것
- [ ] Play Console 개발자 계정 생성($25) + 신원 확인
- [ ] 테스터 20명 모집 → 14일 비공개 테스트
- [ ] 서명 keystore 생성 (비밀번호가 필요하므로 본인 직접)
- [ ] PWABuilder 로 AAB 생성 → `.well-known/assetlinks.json` 의 지문 자리표시자 교체
- [ ] 스토어 자산: 폰 스크린샷, 그래픽 이미지 1024×500, 짧은 설명 80자, 자세한 설명 4000자
- [ ] 콘텐츠 등급 설문 · 데이터 보안 양식("수집 안 함") · 개인정보처리방침 URL(`/privacy`)

*updated 2026-08-21*
